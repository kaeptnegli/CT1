\input{preamble-instruction.tex}

\begin{document}
\begin{instructionblock}{\imagewidth}%
    \textbf{ldm rn<!>, \{ registers \}}\hfill\textit{T1}\\
    rn $\in$ \baseregisters\\
    registers $\subseteq$ \baseregisters\\
    <!> also load rn\\
    \binaryoperation{%
        \bitgroup{asm-blue}     {1100}%
        \bitgroup{asm-grey}     {1}%
        \bitgroup{asm-pink}     {nnn}%
        \bitgroup{asm-red}      {rrrrrrrr}%
    }
    {}\\
    for (k = 0; k < \#regs; k++)\{\\
    regs[k] = *((uint32\_t)(rn + k));\\
    \}
\end{instructionblock}
\end{document}
